@model IEnumerable<BioRec___Empleados.Models.Proveedor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Registar un proveedor nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Nombre proveedor
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.nombreProveedor)
                </td>
                <td></td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.idProveedor">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.idProveedor">Details</a> |
                </td>
            </tr>
        }
    </tbody>
</table>
